apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend_deployment
spec:
  replicas: 4
  selector:
    matchLabels:
      app: backend_app
  template:
    metadata:
      name: backend
      labels:
        app: backend_app
    spec:
      containers:
        - name: backend
          image: apu133/cash-tracker-backend:0.2
          ports:
            - containerPort: 5000