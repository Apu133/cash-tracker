apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  replicas: 4
  selector:
    matchLabels:
      app: frontend-app
  template:
    metadata:
      name: frontend
      labels:
        app: frontend-app
    spec:
      containers:
        - name: frontend
          image: apu133/cash-tracker-frontend:0.2
          ports:
            - containerPort: 3000